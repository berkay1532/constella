# Rust / Soroban
/target/
**/target/
*.wasm
# (Cargo.lock is kept — recommended for contract workspaces)

# Node / web UI
node_modules/
dist/
.next/
*.log

# Env / secrets
.env
.env.*
!.env.example
*.key
identity-*.json

# OS
.DS_Store
Thumbs.db

# Local Claude settings (personal, not shared)
.claude/settings.local.json

# Soroban test snapshots
test_snapshots/
**/test_snapshots/

# macOS/iCloud duplicate artifacts
* 2.*
*\ 2.*

# ZK build artifacts (ptau/zkey/r1cs/wasm/witness — regenerate via zk/build.sh)
zk/build/

# Instawards / SCF application drafts — local-only, never pushed
instawards/

# Personal files (not part of the project)
background_cv.pdf
